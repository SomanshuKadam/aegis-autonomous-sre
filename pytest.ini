[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    unit: isolated deterministic tests
    contract: API and schema contract tests
    integration: tests requiring service or database integration
    e2e: end-to-end acceptance tests
    replay: read-only fixture replay tests
