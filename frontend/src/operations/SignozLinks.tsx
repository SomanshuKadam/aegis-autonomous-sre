type Links = Record<string, {url: string | null; reason: string | null}>;
export function SignozLinks({links}: {links: Links}) { return <section className="panel detail-card" aria-label="SigNoz context"><h2>Observability context</h2>{Object.entries(links).map(([name, link]) => link.url ? <a className="context-link" key={name} href={link.url} target="_blank" rel="noopener noreferrer">Open {name} in SigNoz <span>↗</span></a> : <div className="context-unavailable" key={name}><b>{name}</b><small>{link.reason ?? "Unavailable"}</small></div>)}</section>; }
